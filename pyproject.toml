[project]
name = "ai-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.6.0",
]

[tool.uv.workspace]
members = [
    "openai/simple-chatbot-with-tool-calls",
]
